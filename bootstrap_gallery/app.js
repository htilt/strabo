var main = function(){
	//if user clicks on a thumbnail, expand that thumbnail
	$(img).click(){
	
	}
	
	//if user hovers over a thumbnail, fade image and display metadata over thumbnail
	$(img).hover(){
	
	}
}

$(document).ready(main);